package com.example.shop.config;

// File này giữ lại để tránh compile error nếu có reference cũ.
// Có thể xóa an toàn nếu không có nơi nào import.
public class ApplicationContextProvider {
    private ApplicationContextProvider() {}
}
